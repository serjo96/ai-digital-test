# Роли моделей и кода — этап 3, офлайн-интеграция

Обновлено: 2026-09-09. Реальных вызовов моделей приложения **0**. Sol/Astra выбраны пользователем; доступ аккаунта, фактическое качество и задержка **не проверялись**. По инструкции пользователя любые модельные запросы отложены до его явного сообщения, что ключ добавлен. Установка SDK и чтение документации не являются модельными прогонами.

| Шаг | Исполнитель | Назначение и граница | Последствие ошибки / защита |
|---|---|---|---|
| Загрузка, деньги, исходы строк | Код | Схема, точные суммы, сохранение оригиналов и учёт 220 строк | Потеря строки или неверная цена; инварианты и тесты |
| Явные факты, варианты, кандидаты | Код B1-v2 | Поддержанные формы, ограничители, совместимость каждой пары группы | Ложное объединение; сохранение Pro/поколения/вариантов и unknown |
| Дополнительное семантическое извлечение | OpenAI `gpt-5.6-sol`, reasoning low | Только выбранные проблемные строки; дополнительные структурированные наблюдения с цитатами | Выдуманное число, чужой предмет или снятое условие; строгая схема, локальные проверки и review |
| Тип / категория | Sol в том же extraction-запросе | Найти явное свидетельство типа в заголовке/specs; категория по закрытой taxonomy | Аксессуар может стать устройством; проверка явного типа кодом и запрет перезаписи уверенного решения |
| Сложные пары, отдельный эксперимент | Sol против `gpt-6-astra`, одинаковое reasoning low | Только review-пары B1, одинаковые входы и промпт; по умолчанию выключено | Модельный merge не обходит кодовую совместимость; рекомендации сохраняются отдельно |
| Согласование и удержание | Код | Scope, условия, интервалы округления, conflict/incomparable, очередь review | Модель не выбирает авторитетного поставщика и не разрешает конфликт голосованием |
| Генерация и проверка утверждений | Этап 4, не реализованы | Astra — кандидат для будущего сравнения verifier, не уже измеренный проверяющий | Отдельный будущий эксперимент с поддержанными и искажёнными claims |

Почему модель: фрагмент требует выбора смысла, предмета и применимого атрибута, а не только нахождения числа. Код сохраняет узкие проверяемые границы принятия результата. Это пока гипотеза о полезности AI, а не доказанный выигрыш относительно нескольких дополнительных правил. Если live-эксперимент не покажет пользы, принятым остаётся B1-v2.

## Реальный объём v1

Разрешены пять новых атрибутов: `bluetooth_supported`, `wireless_frequency` (GHz, radio_link), `colour_temperature_count`, `size_count`, `compatible_model` (compatible_device). Источник, число и поддержанные условия проверяет код. Число `4` не принимается из `2.4GHz`; `AeroBuds` не принимается вместо полного `AeroBuds Pro` в совместимости. Остальные характеристики B1 сохраняются без переписывания. Новые неподдержанные поля/форматы, отрицания и пределы остаются unknown/error, а не расширяют словарь автоматически.

Для типа требуется цитата с буквальным типом, распознаваемым существующим словарём. Вывод «AeroBuds означает earbuds» по внешним знаниям запрещён. Немецкая строка без явного названия типа может остаться в review; `18h` не превращается в `20h with case`, неопределённые `12 W` не становятся `12 W output`. Этот консервативный предел и отсутствие общего entailment-verifier намеренны; наличие цитаты не гарантирует смысловую правильность любой модели.

## Адаптеры и воспроизведение

`AiProvider` задаёт структурированный запрос/ответ и нейтральные ошибки. `ProviderRegistry` подключается через Nest DI. OpenAI SDK используется только в `OpenAiAdapter`; цены, eval, schema/evidence, кэш и retry находятся вне адаптера. OpenAI использует Responses, strict JSON Schema, `store:false`, без tools, standard service tier; SDK-retries выключены, общий слой допускает до двух повторов transient-ошибки и 60 секунд на попытку.

Рабочий адаптер Ollama сейчас не реализован. Для его добавления нужны реализация контракта, регистрация фабрики и контрактные тесты; pipeline не должен импортировать SDK нового провайдера. Неподдерживаемые возможности следует отклонять явно, а не заменять строгий выход обычным текстом. Тестовый адаптер существует только в тестах, не выбирается production CLI.

Каждый live-прогон требует нового каталога кэша. Replay проверяет ключ и целостность ответа, повторяет локальную валидацию и не обращается к провайдеру. Ключ включает провайдера/endpoint, модель, параметры, точный вход, промпт и JSON Schema. `ai.json` сохраняет отдельные запросы по их хэшам, ответы, usage, статус валидации и время; секретные заголовки и SDK error bodies не сохраняются. Искусственные ответы имеют происхождение test и запрещены в реальной benchmark-истории.

## Цена и задержка

Проверены официальные документы [Sol](https://developers.openai.com/api/docs/models/gpt-5.6-sol), [Astra](https://developers.openai.com/api/docs/models/gpt-6-astra), [Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs) и [тарифы](https://developers.openai.com/api/docs/pricing). Обе модели поддерживают Responses и Structured Outputs; это не подтверждение доступа конкретного аккаунта.

Сохранённые тарифы standard на 2026-09-09, USD за миллион токенов, до 272000 входных токенов:

| Модель | Input без кэша | Cache read | Cache write | Output |
|---|---|---|---|---|
| Sol | 4 | 0.40 | 5 | 20 |
| Astra | 10 | 1 | 12.50 | 50 |

Пример только для масштаба: 1000 входных токенов без кэша и 500 выходных — $0.014 для Sol и $0.035 для Astra. Это арифметическая оценка, не измеренная стоимость запроса или всего feed. Перед live проверить актуальность тарифов; конфигурация фиксирует дату и источник. Cache-write учитывается отдельно. При отсутствующем usage, неизвестной цене, cache-write breakdown или превышении поддержанного контекста cost = N/A. Reasoning входит в output usage. Неудачная попытка без usage делает общий расход неизвестным, а не нулевым.

Фактическая задержка Sol/Astra, качество и итоговая стоимость B2 — N/A. Длительность искусственного транспорта не используется как оценка модели. Будущий выбор между моделями основан сначала на безопасности и качестве одинаковых случаев, затем на задержке и стоимости. Unknown-связи AeroBuds пока не дают эталона правильности модельных matching-рекомендаций.
